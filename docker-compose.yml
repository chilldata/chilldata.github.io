version: '3'

services:
  jekyll:
    image: jekyll/jekyll:latest
    container_name: chilldata-blog
    command: jekyll serve --watch --livereload --force_polling --host 0.0.0.0
    ports:
      - "4000:4000"
      - "35729:35729"
    volumes:
      - .:/srv/jekyll

